/** "ALL PLAYERS READY" - appears directly over the still-live game screen, no panel/box behind it. */
export function PlayersReadyText() {
  return (
    <div className="bt-players-ready" aria-hidden="true">
      <span>ALL PLAYERS READY</span>
    </div>
  );
}
